import React from 'react'

export default function App() {
  return (
    <div style={{ padding: '24px', color: '#eaeaea' }}>
      <h1>Chaos Control — Smart Scheduler</h1>
      <p>If you can see this, the build & deploy are working ✅</p>
      <p>We can layer the full scheduler UI back in next commit.</p>
    </div>
  )
}
